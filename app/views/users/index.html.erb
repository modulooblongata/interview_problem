<h1>Our Users</h1>
<p>Take a look at our lovely users.</p>
<ul>
  <% @users.each do |user| %>
    <li>
      <%= link_to user.first_name, user %>: <%= user.email %>
    </li>
  <% end %>
</ul>

<p><%= link_to "Make Another User", new_user_path %></p>

<!-- There needs to be an 'index' view that responds with all of the Users in system -->